# Absolute origin baked into the static OpenGraph tags in index.html. The server overrides these
# per request (see server/site.ts), so this only matters for the un-served build output.
VITE_BASE_URL=http://localhost:5173
